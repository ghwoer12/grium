<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ssafy.gallary">

	<insert id="insert" parameterType="GallaryDTO">
		insert into BOARD_TB(GONE_ID,
		SERVER_PHOTO, PHOTO_PATH,ORIGIN_PHOTO,WRITER, COUNT, SECRET)
		values(#{gone_id},#{server_photo}, #{photo_path}, #{origin_photo},
		#{writer}, 0, #{secret})
	</insert>
	
</mapper>